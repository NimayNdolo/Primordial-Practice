const body = document.querySelector('body')
const button = document.querySelector('button')
const colors = ['red', 'blue', 'green', 'yellow']

button.addEventListener('click to change', changeBackground);

function changeBackground() {
    
}